spring.application.name=demo

# Port Configuration
server.port=9001
server.forward-headers-strategy=framework 

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/complaint_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Use MySQL8Dialect for modern MySQL versions to ensure compatible SQL generation
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration
jwt.secret=9a4f2c8d3b7a1e5f8g9h0i1j2k3l4m5n6o7p8q9r0s1t2u3v4w5x6y7z8a9b0c1d
jwt.expiration=36000000

# Swagger
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# --- CRITICAL STARTUP FIXES ---
spring.main.allow-bean-definition-overriding=true
spring.datasource.hikari.initialization-fail-timeout=0

# This is the most important line for data.sql:
# It forces Hibernate to create tables FIRST, then runs data.sql
spring.jpa.defer-datasource-initialization=true
# Ensures SQL scripts run even if the database is already initialized
spring.sql.init.mode=always